/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',           // Enables `next export`
    trailingSlash: true         // Optional: ensures /about/ instead of /about
};

export default nextConfig;